好，我把两边仓库与官方文档核对后的修改都**并入到你的原稿**，做成一版「核验版」报告，能直接贴到 README/设计文档里用。

---

# Cline 工具集在 WebGAL 中的应用分析（核验版）

> 依据：Cline 官方 **Tools Reference Guide** 与 **Remote Browser Support**；WebGAL 官方 GitHub 与文档（脚本语法、目录结构、Terre 预览与 `config.txt` 配置等）。关键出处已在文末与文内标注。([docs.cline.bot][1])

## 一、Cline 核心工具清单（按官方文档修订）

### 1) 文件操作（File Operations）

* `write_to_file`：创建/覆盖文件
* `read_file`：读取文件
* `replace_in_file`：**定点替换**（比“整文件读取-改写”更稳，推荐在批量替换场景使用）
* `search_files`：正则搜索
* `list_files`：列目录
  ([docs.cline.bot][1])

### 2) 终端/代码（Terminal Operations）

* `execute_command`：执行 CLI 命令
* `list_code_definition_names`：提取代码定义（面向主流语言 AST，用于 DSL 时效用有限）
  ([docs.cline.bot][1])

### 3) MCP 集成（Model Context Protocol）

* `use_mcp_tool`：调用 MCP 服务器提供的工具
* `access_mcp_resource`：访问 MCP 资源

> 注：后续把 WebGAL 校验器做成 MCP Server 时，Cline 通过这两项使用。
> ([docs.cline.bot][1])

### 4) 交互工具（Interaction）

* `ask_followup_question`：追问澄清
* `attempt_completion`：呈现最终结果
  ([docs.cline.bot][1])

### 5) 远程浏览器（Browser）

* “Remote Browser Support” 让 Cline 通过受控的 Chrome 实例进行浏览器自动化（打开页面、点击、截图、读 console 等）。

> 文档不拘泥于具体函数名，但能力是官方支持的，日常可称为 *browser action*。
> ([docs.cline.bot][2])

---

## 二、工具在 WebGAL 中的适配矩阵（更新版）

| Cline 工具                                       | WebGAL 适用性 | 用途                                            | 优先级    | 适配要点                                            |
| ---------------------------------------------- | ---------- | --------------------------------------------- | ------ | ----------------------------------------------- |
| **read_file**                                  | ✅ 完全适用     | 读取 `.txt` 剧本                                  | 🔥 高   | 仅限项目沙箱                                          |
| **write_to_file**                              | ✅ 完全适用     | 生成/修改脚本                                       | 🔥 高   | 写入前展示 diff                                      |
| **replace_in_file**                            | ✅ 完全适用     | 批量/定点替换                                       | 🔥 高   | 比整文件覆写更安全                                       |
| **list_files**                                 | ✅ 完全适用     | 列出 `game/scene`、`background`、`figure`、`bgm` 等 | 🔥 高   | **路径过滤**到项目根下                                   |
| **search_files**                               | ✅ 完全适用     | 搜索台词/指令                                       | ⚡ 中    | 支持通配/正则                                         |
| **list_code_definition_names**                 | ⚠️ 半适用     | 提取“场景/标签”                                     | ⚡ 中    | 面向主流语言 AST；对 WebGAL DSL 建议用自定义解析替代              |
| **execute_command**                            | ⚠️ 需限定     | 启动/构建引擎                                       | ⚡ 中    | **白名单**（如 `yarn dev` / `yarn build`），禁止任意 Shell |
| **（Remote）browser**                            | ✅ 可用       | 预览自动化/截图                                      | 💡 低-中 | 指向本地预览 URL（localhost）                           |
| **ask_followup_question / attempt_completion** | ✅ 完全适用     | 人在回路                                          | 🔥 高   | ——                                              |
| **use_mcp_tool / access_mcp_resource**         | ✅ 可用       | 扩展校验/资源服务                                     | 💡 低   | 适合封装 WebGAL 校验器                                 |

> 说明：Cline 工具官方清单与能力以其文档为准，避免在文档中固化到某个源码路径；官方 repo 也明确支持“用浏览器、执行命令、创建/编辑文件、使用 MCP 等”。([docs.cline.bot][1])

---

## 三、WebGAL 关键点（语法 / 目录 / 预览）

* **剧本与入口**：场景以 `.txt` 为单位；入口通常是 `game/scene/start.txt`。可通过 `choose / changeScene / callScene` 实现跳转与分支。([docs.openwebgal.com][3])
* **对话语法**：`角色: 对话;`（建议每句结尾加分号，旁白可写 `:旁白文本;`）。([docs.openwebgal.com][4])
* **背景/立绘**：资源放在固定目录：`game/background`（背景）、`game/figure`（立绘）；切换示例 `changeBg: testBG03.jpg;`，需要立即执行下一句可加 `-next`。([docs.openwebgal.com][5])
* **分支**：`choose: 选项文本:章节文件名 | 选项文本:章节文件名;`（以 `|` 分隔；以分号结束）。([docs.openwebgal.com][6])
* **音频**：`bgm: 文件名;`，支持 `-volume`、`-enter` 参数。([docs.openwebgal.com][7])
* **配置**：`game/config.txt` 里可编辑游戏信息与常规配置。([docs.openwebgal.com][8])
* **预览方式**：

  1. **WebGAL Terre**（推荐）：启动后会在浏览器打开编辑器，默认 `http://localhost:3001/`。 ([docs.openwebgal.com][9])
  2. **源码形态**：在仓库/模板中运行开发服务器（如 `yarn dev`），再用浏览器打开本地地址；Cline 的 `execute_command` 可白名单化启动。

  > 不存在官方的 `webgal-terre preview` CLI 子命令，应使用上述两种方式之一。([docs.openwebgal.com][9])

---

## 四、WebGAL 专用工具（建议新增）

> 以 **MCP** 或内置工具的形式提供，保持“轻量 + 白名单 + 沙箱”。

1. **validate_script** —— 脚本语法/资源引用校验

* 功能：检查分号、未知指令、资源存在性（基于 `game/background|figure|bgm|scene` 目录）等；
* 形态：优先做成 MCP Server（Cline 用 `use_mcp_tool` 调用）。([docs.cline.bot][1])

2. **list_project_resources** —— 项目资源聚合清单

* 功能：一次返回背景/立绘/BGM/场景等，等价于对多个目录的 `list_files` 聚合；
* 依据：目录命名以 WebGAL 文档为准。([docs.openwebgal.com][5])

3. **generate_character_profile** —— 角色配置生成

* 功能：按项目约定把角色注册信息写入配置（例如合并到 `config.txt` 或独立角色清单）；
* 注意：与 Terre/项目模板的习惯保持一致（文件位置因模板而异）。([github.com][10])

4. **preview_scene** —— 场景直达预览

* 做法：更新起始场景/入口或注入跳转；随后**白名单**启动本地 dev（`yarn dev`），然后用“Remote Browser”打开 `localhost` 并执行最小化操作（点击开始、截图等）。([docs.cline.bot][2])

---

## 五、安全限制（社区项目“要轻且稳”）

* **命令白名单**（`execute_command`）：仅允许如 `yarn dev` / `yarn build` / `npm run lint` 这类固定命令；拒绝任意 Shell。
* **路径沙箱**：所有文件操作限定在“用户选择的 WebGAL 项目根”内，拒绝 `..`/绝对路径/URL。
* **浏览器访问域**：仅允许本地预览（`http://localhost:*` / `http://127.0.0.1:*`）。
* **写入二次确认**：所有变更在 UI 中展示 diff，确认后再落盘。
* **最小日志**：本地可见即可，默认不外发。

---

## 六、典型应用场景（按官方语法微调）

### 场景 1：📝 生成场景脚本

```text
用户：创建一个海滩约会场景，包含两个分支
```

**调用链（示意）**

1. 列资源：`list_files(game/background)` → 有 `beach.jpg`
2. 写脚本（新建 `game/scene/beach_date.txt`）：

```txt
changeBg: beach.jpg -next;
bgm: beach_bgm.mp3 -enter=1500;

雪乃: 海风真舒服呢;
由比滨: 要不要去堆沙堡?;

choose:去堆沙堡:SandCastle.txt|在海边散步:BeachWalk.txt;
```

3. `validate_script` → `attempt_completion: "已创建 beach_date.txt"`
   （choose 语法按官方文档：**选项文本:章节文件名**，以 `|` 分隔并以分号结尾）([docs.openwebgal.com][6])

### 场景 2：🔍 批量替换台词（更推荐 `replace_in_file`）

1. `search_files(game/scene, regex="你好", file_pattern="*.txt")`
2. 对匹配文件逐个 `replace_in_file({ path, find:"你好", replace:"您好" })`
3. `ask_followup_question` 继续处理剩余文件
   （`replace_in_file` 为官方列出工具）([docs.cline.bot][1])

### 场景 3：🎮 自动化预览测试

1. `execute_command("yarn dev")`（白名单命令）
2. Remote Browser：打开 `http://localhost:<port>` → 点击“开始” → 截图保存
   （使用官方的远程浏览器支持能力）([docs.cline.bot][2])

### 场景 4：📊 项目结构分析

* `list_files(game/figure)` 得到立绘清单
* `list_files(game/scene)` 得到场景脚本
* （若需“标签/锚点”）用自定义解析或 `validate_script` 的 AST 输出代替 `list_code_definition_names`

---

## 七、与原生 Cline 的对比（保持轻量）

| 维度   | Cline（代码开发）      | WebGAL Agent（本项目）   |
| ---- | ---------------- | ------------------- |
| 主要工具 | 文件/终端/MCP/交互/浏览器 | 同左 + WebGAL 校验/资源聚合 |
| 代码解析 | AST（主流语言）        | DSL 解析（自研/校验器/MCP）  |
| 浏览器  | 远程浏览器调试          | 预览与回归截图             |
| 安全   | 用户批准命令           | 再加白名单/路径沙箱          |
| 完成标准 | 构建/测试通过          | 语法正确 + 本地预览可运行      |

---

## 八、实施路线图（阶段化）

* **Phase 1（MVP）**：
  只接入基础文件工具：`read_file / write_to_file / replace_in_file / search_files / list_files / ask_followup_question / attempt_completion`。
* **Phase 2（增强）**：
  新增 `validate_script`（MCP 或内置）、`preview_scene` 流程（dev + 浏览器自动化）。
* **Phase 3（完善）**：
  批量改写/项目资源管理、截图回归；必要时再加极薄代理（不改变前端契约）。

---

## 主要依据（部分）

* **Cline 工具清单**（含 `replace_in_file`、MCP、交互工具与范例）：Cline Tools Reference Guide。([docs.cline.bot][1])
* **Cline 远程浏览器**：Remote Browser Support；Cline 博文/发行说明也多次强调本地 Chrome 集成。([docs.cline.bot][2])
* **WebGAL GitHub/主页**：项目总览与 VS Code 插件提示。([github.com][10])
* **WebGAL Terre（本地编辑器）**：安装即本地服务，默认 `http://localhost:3001/`。 ([docs.openwebgal.com][9])
* **WebGAL 配置与目录/语法**：`game/config.txt`；背景/立绘目录；对话、`changeBg`、`-next`；`choose` 分支；场景与入口说明。([docs.openwebgal.com][8])

---

**结论**：在“社区项目，尽量轻”的前提下，Cline 的通用工具 **70% 可直接复用**；其余 **30% 通过“白名单命令 + 路径沙箱 + WebGAL 专用校验/预览工具（优先 MCP 化）”** 即可补齐。整体保持 Cline 的“计划→工具→人在回路”的哲学，同时对 WebGAL DSL 与预览链路做最小而明确的适配。

[1]: https://docs.cline.bot/exploring-clines-tools/cline-tools-guide "Cline Tools Reference Guide - Cline"
[2]: https://docs.cline.bot/exploring-clines-tools/remote-browser-support?utm_source=chatgpt.com "Remote Browser Support"
[3]: https://docs.openwebgal.com/en/tech/?utm_source=chatgpt.com "WebGAL Technology Introduction"
[4]: https://docs.openwebgal.com/webgal-script/dialogue.html?utm_source=chatgpt.com "对话"
[5]: https://docs.openwebgal.com/webgal-script/bg-and-figure.html?utm_source=chatgpt.com "背景与立绘"
[6]: https://docs.openwebgal.com/webgal-script/scenes.html?utm_source=chatgpt.com "场景与分支"
[7]: https://docs.openwebgal.com/webgal-script/audio.html?utm_source=chatgpt.com "音频"
[8]: https://docs.openwebgal.com/en/config.html?utm_source=chatgpt.com "Edit Game Configuration | WebGAL Doc"
[9]: https://docs.openwebgal.com/en/getting-started.html?utm_source=chatgpt.com "How to start making a WebGAL?"
[10]: https://github.com/OpenWebGAL/WebGAL?utm_source=chatgpt.com "OpenWebGAL/WebGAL - 全新的网页端视觉小说引擎 - GitHub"
